apiVersion: v1
kind: Service
metadata:
  name: harness-demo-app
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
      # Mapped to host port 30080 by the kind extraPortMappings config.
      nodePort: 30080
