apiVersion: v1
kind: Service
metadata:
  name: harness-demo-app
  labels:
    app.kubernetes.io/name: harness-demo-app
    app.kubernetes.io/part-of: harness-fde
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: harness-demo-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
